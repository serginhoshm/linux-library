# openSUSE Leap 16.0

Compatibilidade confirmada para:
- openSUSE Leap 16.0

## Scripts disponíveis
1. `flatpak-install.sh`
2. `flameshot-install.sh`
3. `ftp-setup.sh`
4. `nfs-client.sh`
5. `nfs-server.sh`
6. `plex-firewall-config.sh`
7. `samba-config.sh`
8. `signal-install.sh`
9. `swap-config.sh`
10. `transmission-config.sh`

## Uso rápido

```bash
chmod +x *.sh
sudo ./flatpak-install.sh
sudo ./flameshot-install.sh
```

## Observações
- Baseado em `zypper` e `systemd`.
- Scripts adaptados para openSUSE Leap 16.0.
- Use `sudo` sempre que o script necessitar de acesso administrativo.

